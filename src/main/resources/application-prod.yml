spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root

  sql:
    init:
      mode: embedded
      schema-locations: classpath:props/schema-prod.sql
      data-locations: classpath:props/data.sql